// Dynamic Configuration
// This file is loaded first to inject runtime config if needed
window.MEDGEMMA_CONFIG = {
    // Leave empty to default to window.location.origin calculation in app.js
    // apiBaseUrl: "http://specific-url.com" 
};